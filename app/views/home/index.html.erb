<header>
  <h1>Suggestion Box</h1>
</header>

<div>
  <h2>Recent Suggestions</h2>
  <% @suggestions.each_with_index do |s,i| %>
    <p><%= link_to "#{i + 1}. #{s.target} - #{s.title}", suggestion_path(s.id) %></p>
  <% end %>
</div>