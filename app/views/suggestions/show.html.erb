<h2><%= @suggestion.title %></h2>

<p>Target: <%= @suggestion.target %></p>
<p>Suggestion:</p>
<p><%= @suggestion.body %></p><br>
<p>Comments:</p>
<% @comments.each do |c| %>
  <p><%= User.find(c.owner_id).first_name %> <%= User.find(c.owner_id).last_name %> said:<br><%= c.body %></p>
<% end %>

<%= link_to 'Go Home', root_path %>